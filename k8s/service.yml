apiVersion: v1
kind: Service
metadata:
  name: kubutw
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kubutw"
    port: 8080
  selector:
    app: kubutw
