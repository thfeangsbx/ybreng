apiVersion: v1
kind: Service
metadata:
  name: cobkuw
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cobkuw"
    port: 8080
  selector:
    app: cobkuw
